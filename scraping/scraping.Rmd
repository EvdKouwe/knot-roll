
```{r packages, echo=FALSE}
library(tidyverse)
library(rmarkdown)
library(rio)
```

```{r import and reset button}
# reset for a clean start
keep(covid19, world_pop, sure = TRUE)
```

```{r starting from complete scratch with rvest}
#i added some extra test
```
